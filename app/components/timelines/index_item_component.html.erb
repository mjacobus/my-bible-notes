<div class="list-item position-relative">
  <%= render menu %>
  <%= render attribute(timeline.name).with_icon(icon_name).with_label(:name).without_label.wrap_with(:div, class: :h5) %>
  <%= render attribute(t('app.messages.x_timeline_entries', number: timeline.entries.count)).with_label(:number_of_timeline_entries).without_label %>
</div>
