<%= render breadcrumb %>
<%= render flash %>
<%= render menu %>

<%= content_for(:title) { "#{scripture.username}: #{scripture.title}" } %>
<%= render attribute(scripture.title).with_label(:title) %>
<%= render attribute(scripture.description).with_label(:description).without_label.wrap_with(:small) %>
