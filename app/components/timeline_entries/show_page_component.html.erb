<%= render breadcrumb %>
<%= render flash %>
<%= render menu %>

<%= render attribute(record.title).with_label(:title) %>
<%= render TimelineEntries::TimeRangeComponent.new(entry: record).with_label.without_icon %>
<%= render attribute(record.explanation).with_label(:explanation) %>
<%= render attribute(t(record.confirmed.to_s)).with_label(:confirmed) %>
