<div class="list-item position-relative">
  <%= render menu %>
  <%= render attribute(entry.title).with_icon(:alarm).with_label(:title).without_label.wrap_with(:div, class: :h5) %>
  <%= render attribute(entry.formatted_from_year).with_icon(:calendar).with_label(:title).without_label %>
  <%= render attribute(entry.formatted_to_year).with_icon(:calendar).with_label(:title).without_label %>
  <!-- confirmed-->
  <!-- explanation-->
  <!-- date_complement-->
</div>
