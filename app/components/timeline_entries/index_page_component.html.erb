<%= render breadcrumb %>
<%= render flash %>
<%= render menu %>

<% entries.each do |entry| %>
  <%= render TimelineEntries::IndexItemComponent.new(entry: entry) %>
<% end %>

<%= render pagination %>
